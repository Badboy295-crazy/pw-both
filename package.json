{
  "name": "studyhub-root",
  "version": "2.0.0",
  "description": "Unified Study Hub Bot & WebApp Root Runner",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && node index.js",
    "build": "cd server && npm install",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
